# Copie para `.env` no diretório de deploy da VPS.
# O CI sobrescreve IMAGE_TAG a cada deploy (com a tag recém-criada).
IMAGE_TAG=latest

# Porta no host (só localhost) que o Nginx vai proxiar. 3003 está livre na VPS atual.
BACKEND_PORT=3003
